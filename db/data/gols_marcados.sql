SELECT 
	SUM(GT.TOTAL_GOLS_BRASIL) AS TOTAL_GOLS_MARCADOS
FROM
	(
	SELECT
		SUM(GOL_CASA) AS TOTAL_GOLS_BRASIL
	FROM
		PARTIDA
	WHERE
		ID_SELECAO_CASA = 1
UNION ALL
	SELECT
		SUM(GOL_FORA) AS GOLS_FORA
	FROM
		PARTIDA
	WHERE
		ID_SELECAO_FORA = 1
) AS GT